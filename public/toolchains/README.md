# Browser toolchains

These content-addressed compiler, linker, sysroot, and runtime assets execute locally through
Wasmer or the Java WasmGC adapter. They are distributed by the six `@wasm-oj/toolchain-*` packages. Browser and server hosts
must receive the packages' explicit `browserSource(baseUrl)` or `serverSource()` values; there is no
default asset base, bundled toolchain, CDN lookup, or filesystem search. A browser `baseUrl`
identifies the HTTP directory containing the declared asset basenames. WASM-OJ appends the pinned
lowercase SHA-256 as a `sha256` query parameter, verifies byte length and digest before use, and
rejects ambiguous asset ownership. The bundled service worker intercepts only same-origin GET
requests carrying exactly one valid `sha256` query and caches a response only after verifying its
bytes; a cross-origin asset host controls its own caching. Source code is never part of an asset
request.

The independent source packages are `@wasm-oj/toolchain-clang`,
`@wasm-oj/toolchain-rust`, `@wasm-oj/toolchain-go`, `@wasm-oj/toolchain-python`, and
`@wasm-oj/toolchain-javascript`, and `@wasm-oj/toolchain-java`.

The `.gz.bin` suffix is intentional. It prevents static hosts from treating the stored gzip artifact as HTTP `Content-Encoding: gzip`; the Worker receives the compressed bytes and performs exactly one explicit decompression before Wasmer instantiation.

| Asset | Upstream | Source revision | Build/runtime form |
| --- | --- | --- | --- |
| `typescript-7.0.2.wasm.gz.bin` | [microsoft/typescript-go](https://github.com/microsoft/typescript-go) | `2bd066d87f5bafd315be9f40889d0a60b9e58e0b` | Go 1.26.3, `wasip1/wasm` |
| `quickjs-0.15.1.wasm.gz.bin` | [quickjs-ng/quickjs](https://github.com/quickjs-ng/quickjs) | commit `fd0a0210b7be00957751871e7e01b8291268fc29` | [WASI SDK 24.0](https://github.com/WebAssembly/wasi-sdk/tree/d2bea01edcc46f731156a817f710cdd9fc9c1c19), `wasm32-wasip1`; stripped WASM-OJ deterministic bridge |
| `rust-1.91.1-dev.webc.gz.bin` | [olimpiadi-informatica/wasm-compilers](https://github.com/olimpiadi-informatica/wasm-compilers) and [`@yowasp/clang@22.0.0-git20542-10`](https://www.npmjs.com/package/@yowasp/clang/v/22.0.0-git20542-10) | rustc commit `ae62cab6adf0665377d19ffa39daeaf758290431`, Actions run `26545267884`, artifact SHA-256 `ba0096…01c14`; linker archive SHA-256 `6230ea…06009` | One deterministic WebC containing rustc, its `wasm32-wasip1-threads` sysroot, wasm-ld, and linker resources |
| `rust-1.91.1-dev.manifest.json` | generated by `tools/package-rust-webc` | same pinned compiler and linker sources; manifest SHA-256 `d5bbdc…0bce1` | Source, compiler, linker argv, two-stage pipeline, output, and filesystem identities |
| `go-1.26.5-wasip1.webc.gz.bin` | [Go 1.26.5](https://go.dev/dl/) | official source tarball SHA-256 `495be4…dc42` | Deterministic WebC exposing standard `compile` and `link` commands under Wasmer |
| `go-1.26.5-wasip1.stdlib.gz.bin` | generated from the same Go distribution | deterministic digest `aeffc3…80d8` | Canonical archive of all 349 `GOOS=wasip1 GOARCH=wasm` standard-library packages |
| `go-1.26.5-wasip1.manifest.json` | generated by `tools/package-go-webc` | deterministic digest `5d784e…a262` | Source distribution, command atoms, package graph, compiler/linker, and output identities |
| `clang-22.0.0-git20542-10.webc.gz.bin` | [`@yowasp/clang@22.0.0-git20542-10`](https://www.npmjs.com/package/@yowasp/clang/v/22.0.0-git20542-10) | npm archive SHA-256 `6230ea…06009` | One deterministic WebC containing the standard WASI P1 Clang 22 core, LLD, libc++, and `/usr` sysroot |
| `python-3.14.7-wasip1.webc.gz.bin` | [CPython 3.14.7](https://www.python.org/ftp/python/3.14.7/) | source archive SHA-256 `3b48da…27f81`; expanded WebC SHA-256 `a42c98…4b56` | Source-built CPython and pruned standard library, built with WASI SDK 24.0 and stored as deterministic gzip |
| `python-3.14.7-wasip1.manifest.json` | generated by `tools/package-python-webc` | deterministic digest `53a24b…9cd5` | Source/SPDX/toolchain/license identities, exact WASI imports, canonical sysconfig roots, runtime-files archive identity, and output digests |
| `clang-22.0.0-git20542-10.cc1-pins.json` | captured from that package's driver `-###` expansion | deterministic digest `895eee…b1ff` | Frozen C17/C++20 `wasm32-unknown-wasip1` cc1 and LLD command lines per language/optimization pair |
| `clang-22.0.0-git20542-10.manifest.json` | generated by `tools/package-yowasp-clang` | deterministic digest `6382dc…5273` | Source, embedded core/resources, output, command, and filesystem identities |
| `java-teavm-0.13.1.compiler.wasm` | [TeaVM Java compiler](https://github.com/konsoletyper/teavm-javac) plus the WasmGC compiler patch | compiler revision `7e4a44cf521694a4e326e33850dd8aec165eb5c9` | WasmGC compiler with static JavaScript bindings; compile/runtime class libraries are separate assets |
| `java-teavm-0.13.1.compile-classlib.bin` | OpenJDK 21 class library | GPL-2.0 with Classpath exception | TeaVM compile-time class library |
| `java-teavm-0.13.1.runtime-classlib.bin` | OpenJDK 21 class library | GPL-2.0 with Classpath exception | TeaVM runtime class library |

The small TypeScript and QuickJS adapters provide deterministic stdin/stdout protocols so the toolchain does not depend on host filesystem access. Rebuild or refresh assets with:

```sh
pnpm run toolchain:browser:prepare /absolute/path/to/wasi-sdk-24.0-arm64-macos.tar.gz
pnpm run toolchain:rust:prepare /absolute/path/to/rust.tar.br
pnpm run toolchain:go:prepare
pnpm run toolchain:python:prepare /absolute/path/to/Python-3.14.7.tar.xz /absolute/path/to/Python-3.14.7.tar.xz.spdx.json /absolute/path/to/wasi-sdk-24.0-arm64-macos.tar.gz
pnpm run toolchain:clang:prepare
```

Expected SHA-256 digests:

```text
895eee17c63b1112c84adcc4ddbcb38e0c18e53bff15f50a8689b9b82d87b1ff  clang-22.0.0-git20542-10.cc1-pins.json
a3811351bae3289caf096f2d4bb6d2ee5db84b97e05514e87190d36b65c500cf  clang-22.0.0-git20542-10.cpp-debug.pch.gz.bin
ef004fc05ec50af697a37f7c9c814bf62e9676d0de162d6ac35305533939f93f  clang-22.0.0-git20542-10.cpp-release.pch.gz.bin
9bbe41d77c786f1c36169369c909268530b05726e1581683f6edd9a2983eebd0  clang-22.0.0-git20542-10.libcxx-pch.json
6382dcdfb6a2da49032a0e08da3b1fb490eb24432be85c3c12e3e871a5065273  clang-22.0.0-git20542-10.manifest.json
7f10d90b8e52b270f04874641a1d0bf9e94e85b4f6c7573a774cebbc6d32552a  clang-22.0.0-git20542-10.webc.gz.bin
5d784e9ca640b9525e84b598c0beb97ca110ae908568ca45f6441a441d99a262  go-1.26.5-wasip1.manifest.json
aeffc384fdc624544f174ba5fc3c22395717fdbc3c4387d677d20855b6be80d8  go-1.26.5-wasip1.stdlib.gz.bin
70a7e359884b09b2e1a622d6ac5cd6e31c334aab519e6dd80dff5e040a9e09e4  go-1.26.5-wasip1.webc.gz.bin
738a82b7d7e7ff8d98b07bb076785da7035847a166afb8446fd150c4efce6103  java-teavm-0.13.1.compile-classlib.bin
8c37bce1c6fceeaffeffe93e10834309b67c8b6fb58c6f8a24547afe8be67f5d  java-teavm-0.13.1.compiler.wasm
0ed234b66cdde19cb29e6c2865a3636d792d3cf72f102e73a7b3fdede80b79e6  java-teavm-0.13.1.runtime-classlib.bin
53a24b258363a8494af164121111a34b49c85fa0e054627fe131feba6f359cd5  python-3.14.7-wasip1.manifest.json
10027f0e32c77dfa0ce1c413b6cb27307d6744fe5e18e6da8cea738bd020260c  python-3.14.7-wasip1.webc.gz.bin
dc6e02e8610269e61b341331661515616451cec929fd534fa96d1c4f47fbcc9a  quickjs-0.15.1.wasm.gz.bin
d5bbdca994e61888679c5738cb9420649c0854ed0eb5d65468bc67d5d550bce1  rust-1.91.1-dev.manifest.json
cfbdadc67be1315e735aa55bdf8a5a0d00171982a023fefcf7ba586127753887  rust-1.91.1-dev.webc.gz.bin
29c6ee0e46151e2644049ae96162b1b1d46dcb13310e2a928114bb9030c3ea92  typescript-7.0.2.wasm.gz.bin
```

The C/C++ `wasip1` and `wasix` WASM-OJ profiles both consume this one WASI P1 manifest. `wasix` changes artifact, cache, cost-calibration, and runtime-profile identity; it does not select another Clang target, linker command, or runtime implementation. The runner validates the module's actual imports.

`pnpm run toolchain:browser:prepare /absolute/path/to/wasi-sdk-24.0-arm64-macos.tar.gz`
requires the official arm64 macOS WASI SDK 24.0 release archive. It verifies the
archive SHA-256 `aeae9993…b0b3a`, the exact Clang, LLD, strip tool, crt1,
wasi-libc, emulated-signal, libm, and compiler-rt inputs, then builds QuickJS-ng
at the pinned commit. The script strips debug sections, verifies the expanded
Wasm SHA-256 `a098db08…6b20b`, creates deterministic gzip, and publishes both
browser assets atomically.

`pnpm run toolchain:python:prepare …` verifies the official CPython source
archive, its archive-bound SPDX document, and WASI SDK archive before building
the native bootstrap interpreter and `wasm32-wasip1` interpreter from scratch.
It requires `_socket` to be absent, canonicalizes and reads back packaged
sysconfig data, verifies the exact WASI import set and complete license closure,
then executes the package through Wasmer. That smoke reproduces the 10,695,683-
byte `WOJFS002` standard-library archive with SHA-256
`c1acac884af1c86833db5b65f8cc0a2304fce74dea0a05d9b40bde0a4e3e7c0e`.

`pnpm run toolchain:clang:prepare` downloads the fixed npm archive, verifies its archive, compiler-core, and resources digests, rebuilds the WebC deterministically, asks the packaged driver for its exact job expansion, freezes the resulting cc1/link argv, and verifies every output digest. Runtime compilation uses only these verified bytes and fails explicitly if an asset or digest does not match.

`pnpm run toolchain:rust:prepare /absolute/path/to/rust.tar.br` verifies the
exact artifact from the pinned upstream Actions run, downloads and verifies the
pinned YoWASP linker archive, and reconstructs one WebC from the rustc and LLVM
atoms plus their matching sysroot/resource volumes. It normalizes filesystem
timestamps, freezes the exact two-stage linker contract, verifies package and
provenance-manifest digests, and atomically replaces both published assets. The
source build repository pins Rust and LLVM submodules and contains the applied
patches and complete source-traceable build workflow used for the rustc artifact.

`pnpm run toolchain:go:prepare` verifies the official Go 1.26.5 distribution for
the current packaging host and records the official source-distribution URL and
SHA-256 as stable provenance. It invokes the standard Go build to obtain the
`wasip1/wasm` compiler, linker, and complete standard library, then packages and
verifies their WebC command ownership and deterministic archive. Repeating the
packaging operation must reproduce the three published digests above.

TypeScript-Go is licensed under Apache-2.0. QuickJS-ng and the local adapters
are licensed under MIT-compatible terms; the linked WASI SDK, LLVM compiler-rt,
and wasi-libc runtime components retain their upstream licenses. The Rust
compiler and standard-library components retain their upstream Rust licenses.
The Go compiler and standard-library components retain the upstream Go BSD license.
The Clang package retains its package/upstream LLVM licenses. See
`THIRD_PARTY_NOTICES.md` and `licenses/` for the exact distributed notices.
