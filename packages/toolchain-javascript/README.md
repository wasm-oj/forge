# `@wasm-oj/toolchain-javascript`

Digest-pinned TypeScript-Go compiler and QuickJS-ng runtime assets for JavaScript and TypeScript
WASM-OJ projects. Register `browserSource(baseUrl)` or `serverSource()` explicitly; no fallback
source is provided.
