# `@wasm-oj/toolchain-go`

Digest-pinned Go compiler, linker, and WASI standard-library assets for WASM-OJ. Register
`browserSource(baseUrl)` or `serverSource()` explicitly; no fallback source is provided.
