# `@wasm-oj/toolchain-java`

Digest-pinned TeaVM Java/WASI compiler and OpenJDK class-library assets for WASM-OJ.
Register `browserSource(baseUrl)` or `serverSource()` explicitly; no fallback source is provided.
