# `@wasm-oj/toolchain-clang`

Digest-pinned Clang 22, libc++, and PCH assets for C and C++ WASM-OJ builds. Register
`browserSource(baseUrl)` or `serverSource()` explicitly; no fallback source is provided.
