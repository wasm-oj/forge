# `@wasm-oj/toolchain-rust`

Digest-pinned Rust compiler, standard library, and linker assets for WASM-OJ. Register
`browserSource(baseUrl)` or `serverSource()` explicitly; no fallback source is provided.
